# 변수와 자료형

### 변수 (variable)
- 데이터를 저장하기 위해 프로그램에 의해 **이름을 할당받은 메모리 공간**을 의미
  / 변수란 데이터를 저장할 수 있는 메모리 공간
- 저장된 값은 변경될 수 있음 (말 그대로 변하는 수[값])
- 하나의 값을 저장하기 위한 이름이 있는 공간 (공간 == 메모리)

  
  ```age = 20```

    이라고 하면, age라는 변수에 20이라는 값을 저장한 것!
- 변수 앞에 붙는 자료형은 공간의 크기
- 변수를 선언한다 : 변수의 자료형을 선택하고 이름을 정하는 것을 말함

### 변수의 이름 생성 규칙

|제약 사항|예시|
|:---|:---:|
|이름은 영문자 (대소문자), 숫자, 언더스코어(_), 달러($) 로만 구성할 수 있다|g_level(O),count100(O), _master(O), $won(O)|
|이름은 숫자로 시작할 수 없다|27day(X), 1abc(X)|
|이름 사이에는 공백을 포함할 수 없다|27 day(X), 1 a b c(X)|
|자바에서 미리 정의된 키워드(예약어)는 사용할 수 없다|while, int, break, ...|



- 변수 이름은 프로그램에서 계속 사용할 것이기 때문에 사용 목적에 맞게 의미를 잘 부여해서 정확하게 표형할 수 있는 단어로 만드는 것이 좋다

#### 변수명 관례
카멜케이스 : 첫 단어를 제외하고 새로운 단어 시작되는 시 대문자로 표기<br/>
예) 학생수 - noOfStudent
<br/>
자바에서는<br/>
변수, 메서드는 카멜 케이스 / 클래스 등의 타입은 파스칼 케이스 / 상수는 대문자 스네이크 케이스 표기법 권장

1. 카멜 표기법(camelCase)
- 낙타 등에 있는 혹처럼 내려갔다 올라가는 모양이라 하여 지어진 이름
- 단어가 여러개 붙을 때, 앞 단어를 제외한 첫자를 대문자로 표기
- java, C# 등의 언어에서 권장 

ex) dailyUserTable

2. 파스칼 표기법(PascalCASE)
- 모든 단어의 앞글자가 대문자로 시작(단어의 수와 상관 없음)
- 네임스페이스, 이벤트, 프로퍼티, 클래스 네임을 지정할 때 주로 사용
- 클래스 등에서 많이 사용
- 주의) 언어에서는 클래스명의 첫 단어만 대문자로 사용하는 경우가 있음

ex) DailyUserTable

3. 스네이크 표기법(snake_case), 팟홀 표기법(pothole case) 
- 모든 단어가 소문자로 표시
- 다른 의미를 갖는 단어들의 조합에서 각 단어의 구분을 위하여 "_"(언더바)를 붙힘
- 단어 사이의 "_" 가 뱀처럼 보인다고 해서 유래
- 언더바 표기법 이라고도 불리움
- C++에서 권장

ex) daily_user_table

4. 케밥 케이스(kebab-case)
- 케밥이 꼬챙이에 꽂힌 모습과 같은 모양이라 케밥 케이스로 불린다.
- 모든 단어는 소문자로 시작하고 단어와 단어 사이는 "-"(하이픈)으로 연결한다.

ex) java-programming 

5. 헝가리안 표기법
- 접두사에 자료형을 붙힘
- 마이크로소프트 개발자 중 헝가리 프로그래머가 쓰던 변수 명명법
- 현재는 자료형을 쉽게 알아 볼수 있는 다양한 방법들이 많기 때문에 권장하지 않음

 ex) strDailyUserTable

6. 커스텀
- 개발사만의 또는 개발 조직 자체적으로 사용하는 방식 또한 많이 사용

ex) 접두사(두자리)_풀네임(단어)_약어....
접두사 대문자, 단어의 첫글자 대문자, 약어 대문자
ST_User_CD
<br/><br/>

#### 변수명 짓기 팁!
1. 해당 언어에 맞는 변수명 규칙을 먼저 숙지 하기
2. 이름에 의미 넣기 / Database를 DB로 줄이는 건 모두 이해할 수 있으니 괜찮은데 User Name List를 UNL등으로 나만 알아볼 수 있는 임의의 단어로 사용하는 건 지양
3. 함수 이름에 함수가 하는 일을 정확히 드러내기 / 두세가지 일을 하는데 그중 하나만 표현하면 나중에 이름만으로는 파악이 힘들수도 있으니 그럴땐 변경해주기
4. 나에게 맞는 케이스 스타일 활용하기 / 대소문자, -, _, 를 활용하여 함수이름의 가독성 높이기
5. 동사를 적극 활용하기 / is로 시작하는 의문문형태로 지으면 is만 보고 해당이름이 변수임을 알 수 있고 함수는 수행과 관련되어 있기 때문에 해당 함수가 어떤 일을 수행할지 일반동사로 시작하는 함수 이름을 지으면 이해하기 쉬움 // 업데이트인지, 보여주는 것인지, 설정하는 것인지, 가져오는 것인지 직관적인 이름이 됨.
6. 주석을 적극적으로 활용하기
7. 보편적으로 사용되는 이름 짝 맞추어 사용하기 / 주기적으로 사용자데이터를 갱신하는 함수에 대해서 EditUserData, ModifyUserData 보다는 UpdateUserDater를 사용한다던지 get을 사용했으면 이후에는 release혹은 set를 사용하는것이 일반적 , road는 umload와 더 잘 어울리므로 delete등의 다른 단어를 이름으로 사용하는 것은 지양








